# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# App
NEXT_PUBLIC_APP_URL=https://login.purepulse.one

# Google Calendar interview scheduling (OAuth web application)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_CALENDAR_REDIRECT_URI=https://login.purepulse.one/api/settings/google-calendar/callback
# Generate each secret independently with: openssl rand -base64 32
GOOGLE_CALENDAR_TOKEN_ENCRYPTION_KEY=
GOOGLE_CALENDAR_OAUTH_STATE_SECRET=
APP_BASE_URL=https://login.purepulse.one

# Stripe & Global Payouts
STRIPE_SECRET_KEY=sk_test_placeholder
STRIPE_WEBHOOK_SECRET=whsec_placeholder
STRIPE_GLOBAL_PAYOUTS_API_VERSION=2026-07-29.preview
STRIPE_FINANCIAL_ACCOUNT_ID=fa_placeholder

# Stripe Product Price IDs
STRIPE_PRICE_STARTER=
STRIPE_PRICE_GROWTH=
STRIPE_PRICE_PREMIUM=
STRIPE_PRICE_BUSINESS=
STRIPE_PRICE_DEPOSIT=

# Resend (email)
RESEND_API_KEY=
RESEND_WEBHOOK_SECRET=

# Anthropic (AI campaign generation)
ANTHROPIC_API_KEY=
# Cloudflare Access-protected three-server workflow dashboard
NEXT_PUBLIC_HANDOFF_DASHBOARD_URL=https://handoff.relayapp.pro

# Server Handoff integration (Projects issue #25)
# Ingest bearer token: >= 32 random bytes, shared only with the workflow dashboard.
HANDOFF_INGEST_TOKEN=
# Fixed-window ingest rate limit (requests per minute).
HANDOFF_INGEST_RATE_LIMIT=120
# Comma-separated admin email allowlist for handoff actions endpoint.
ADMIN_EMAILS=
